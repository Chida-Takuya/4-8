<!DOCTYPE html>
<html>
	<head>
		<mata charset = "UTF-8">
		<title> 4-8</title>
		<%= stylesheet_link_tag "application", :media => "all" %>
	</head>

	<body>
		<h1> Editing Book</h1>
		<%= form_for(@post) do |f| %>

    	<h4> Title</h4>
    	<%= f.text_field :title %>

    	<h4> Body</h4>
    	<%= f.text_area :body %>

    	<br><%= f.submit 'Update Book' %>
		<% end %>

		<%= link_to "Show", post_path(@post.id), class:'show_#{id}' %> |
		<%= link_to "Back", posts_path(@post.id), class:'back_#{id}' %>
	</body>
</html>