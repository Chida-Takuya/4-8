<!DOCTYPE html>
<html>
	<head>
		<mata charset = "UTF-8">
		<title> 4-8</title>
		<%= stylesheet_link_tag "application", :media => "all" %>
	</head>

  	<body>
    	<p id="notice">successfully!</p>

		<p>
  			<strong>Title:</strong>
  			<%=@post.title %>
		</p>

		<p>
			<strong>Body:</strong>
			<%=@post.body %>
		</p>

		<%= link_to "Edit", edit_post_path(@post.id), class: "edit_#{@post.id}" %> |
		<%= link_to "Back", posts_path(@post.id), class: "back" %>

  	</body>
</html>
